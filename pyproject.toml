[project]
name = "ctae-pre-annotation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["polars", "ruff", "ty", "more-itertools", "lseval"]

[tool.uv.sources]
lseval = { path = "/Users/etg/Repos/lseval", editable = true }